<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
    <select id="memberSelectList" parameterType="com.oke.app.sample.MemberVo" resultType="com.oke.app.sample.MemberVo">
        SELECT TEST AS ID
        FROM TEST
    </select>

    <select id="memberSelect" parameterType="com.oke.app.sample.MemberVo" resultType="com.oke.app.sample.MemberVo">
        SELECT TEST AS ID
        FROM TEST
        WHERE 1=1
        AND TEST = 'test'
    </select>

</mapper>